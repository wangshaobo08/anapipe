bin_PROGRAMS = anapipe

anapipe_LDADD = \
        $(LDFLAGS)

anapipe_SOURCES = main.cpp genjob.cpp genpipe.cpp job.cpp options.cpp pipeline.cpp task.cpp

clean:
	rm -rf .deps Makefile.in Makefile *.o ${bin_PROGRAMS}
